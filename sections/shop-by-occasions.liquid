<!-- Swiper CSS -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" media="print" onload="this.media='all'">
<noscript><link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css"></noscript>

<style>
  @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap');

  .sbo-section {
    font-family: 'Poppins', sans-serif;
    background-color: {{ section.settings.background_color }};
  }

  .sbo-container {
    max-width: var(--page-width);
    margin: 0 auto;
    padding: 0 15px;
    width: 100%;
    box-sizing: border-box;
  }

  .sbo-header {
    text-align: center;
    margin-bottom: 32px;
  }

  .sbo-badge {
    display: inline-block;
    font-size: 13px;
    font-weight: 600;
    color: #f97b22;
    letter-spacing: 0.08em;
    margin-bottom: 8px;
    text-transform: uppercase;
  }

  .sbo-heading {
    font-size: 28px;
    font-weight: 700;
    color: #333;
    margin: 0;
  }

  /* Swiper wrapper */
  .sbo-swiper-wrapper {
    position: relative;
    padding: 0;
  }

  .sbo-swiper {
    overflow: hidden;
  }

  .sbo-swiper .swiper-slide {
    height: auto;
  }

  /* Card */
  .sbo-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-decoration: none;
    color: inherit;
    transition: transform 0.3s ease;
  }

  .sbo-card:hover {
    transform: translateY(-4px);
  }

  .sbo-card-image-wrap {
    width: 100%;
    aspect-ratio: 1 / 1;
    overflow: hidden;
    border-radius: 12px;
    margin-bottom: 14px;
    background: {{ section.settings.card_bg_color }};
  }

  .sbo-card-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 0.4s ease;
  }

  .sbo-card:hover .sbo-card-image {
    transform: scale(1.05);
  }

  .sbo-card-title {
    font-size: 15px;
    font-weight: 600;
    color: #111;
    text-align: center;
  }

  /* Navigation arrows */
  .sbo-nav-prev,
  .sbo-nav-next {
    position: absolute;
    top: 42%;
    transform: translateY(-50%);
    z-index: 10;
    width: 44px;
    height: 44px;
    background: #fff;
    border: 1px solid #e5e5e5;
    border-radius: 50%;
    box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
  }

  .sbo-nav-prev:hover,
  .sbo-nav-next:hover {
    background: #f97b22;
    border-color: #f97b22;
    box-shadow: 0 4px 16px rgba(249,123,34,0.35);
  }

  .sbo-nav-prev svg,
  .sbo-nav-next svg {
    width: 18px;
    height: 18px;
    stroke: #333;
    fill: none;
    transition: stroke 0.3s ease;
    stroke-width: 2.5;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  .sbo-nav-prev:hover svg,
  .sbo-nav-next:hover svg {
    stroke: #fff;
  }

  .sbo-nav-prev { left: -15px; }
  .sbo-nav-next { right: -15px; }

  .sbo-nav-prev.swiper-button-disabled,
  .sbo-nav-next.swiper-button-disabled {
    opacity: 0.35;
    cursor: default;
    pointer-events: none;
  }

  /* Pagination */
  .sbo-swiper-wrapper .swiper-pagination {
    position: relative;
    margin-top: 24px;
  }

  .sbo-swiper-wrapper .swiper-pagination-bullet {
    width: 8px;
    height: 8px;
    background: #ddd;
    opacity: 1;
    transition: all 0.3s ease;
  }

  .sbo-swiper-wrapper .swiper-pagination-bullet-active {
    background: #f97b22;
    width: 24px;
    border-radius: 4px;
  }

  @media (max-width: 749px) {
    .sbo-heading { font-size: 20px; }
    .sbo-header { margin-bottom: 20px; }
    .sbo-container { padding: 0 16px; }
    .sbo-swiper-wrapper { padding: 0; }
    .sbo-swiper { overflow: visible; }
    .sbo-swiper .swiper-wrapper { padding: 0 8px; box-sizing: border-box; }
    .sbo-nav-prev, .sbo-nav-next { display: none; }
    .sbo-card-title { font-size: 14px; }
  }
</style>

<div
  class="sbo-section"
  style="padding-top: {{ section.settings.padding_top }}px; padding-bottom: {{ section.settings.padding_bottom }}px;"
>
  <div class="sbo-container">
    <div class="sbo-header">
      {%- if section.settings.badge_text != blank -%}
        <div class="sbo-badge">{{ section.settings.badge_text }}</div>
      {%- endif -%}
      {%- if section.settings.heading != blank -%}
        <h2 class="sbo-heading">{{ section.settings.heading }}</h2>
      {%- endif -%}
    </div>

    <div class="sbo-swiper-wrapper">
      <button class="sbo-nav-prev" id="sbo-prev-{{ section.id }}" aria-label="Previous">
        <svg viewBox="0 0 24 24"><polyline points="15 18 9 12 15 6"></polyline></svg>
      </button>
      <button class="sbo-nav-next" id="sbo-next-{{ section.id }}" aria-label="Next">
        <svg viewBox="0 0 24 24"><polyline points="9 6 15 12 9 18"></polyline></svg>
      </button>

      <div class="swiper sbo-swiper" id="sbo-swiper-{{ section.id }}">
        <div class="swiper-wrapper">
          {%- for block in section.blocks -%}
            {%- assign card_link = block.settings.link | default: '#' -%}
            <div class="swiper-slide" {{ block.shopify_attributes }}>
              <a href="{{ card_link }}" class="sbo-card">
                <div class="sbo-card-image-wrap">
                  {%- if block.settings.image != blank -%}
                    {{ block.settings.image | image_url: width: 400 | image_tag: loading: 'lazy', class: 'sbo-card-image' }}
                  {%- else -%}
                    {{ 'collection-1' | placeholder_svg_tag: 'placeholder-svg sbo-card-image' }}
                  {%- endif -%}
                </div>
                {%- if block.settings.title != blank -%}
                  <div class="sbo-card-title">{{ block.settings.title }}</div>
                {%- endif -%}
              </a>
            </div>
          {%- endfor -%}
        </div>
        <div class="swiper-pagination"></div>
      </div>
    </div>
  </div>
</div>

<!-- Swiper JS -->
<script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js" defer></script>
<script>
  document.addEventListener('DOMContentLoaded', function () {
    new Swiper('#sbo-swiper-{{ section.id }}', {
      slidesPerView: 2,
      spaceBetween: 12,
      loop: true,
      grabCursor: true,
      pagination: {
        el: '#sbo-swiper-{{ section.id }} .swiper-pagination',
        clickable: true,
      },
      navigation: {
        nextEl: '#sbo-next-{{ section.id }}',
        prevEl: '#sbo-prev-{{ section.id }}',
      },
      breakpoints: {
        600: {
          slidesPerView: 3,
          spaceBetween: 12,
        },
        900: {
          slidesPerView: 5,
          spaceBetween: 16,
        },
        1100: {
          slidesPerView: 6,
          spaceBetween: 20,
        },
      },
    });
  });
</script>

{% schema %}
{
  "name": "Shop By Occasions",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "text",
      "id": "badge_text",
      "label": "Badge Text",
      "default": "🎁 SHOP BY OCCASIONS"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Shop By Occasions"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background Color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "card_bg_color",
      "label": "Card Background Color",
      "default": "#fdf0ec"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding top",
      "default": 48
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding bottom",
      "default": 48
    }
  ],
  "blocks": [
    {
      "type": "occasion_item",
      "name": "Occasion",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Birthday"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Shop By Occasions",
      "blocks": [
        { "type": "occasion_item", "settings": { "title": "Birthday" } },
        { "type": "occasion_item", "settings": { "title": "Anniversary" } },
        { "type": "occasion_item", "settings": { "title": "Wedding" } },
        { "type": "occasion_item", "settings": { "title": "Mother's Day" } },
        { "type": "occasion_item", "settings": { "title": "Father's Day" } },
        { "type": "occasion_item", "settings": { "title": "Christmas" } },
        { "type": "occasion_item", "settings": { "title": "Valentine's Day" } },
        { "type": "occasion_item", "settings": { "title": "Graduation" } }
      ]
    }
  ]
}
{% endschema %}
